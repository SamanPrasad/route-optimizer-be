1. clone the code base from the repo using git clone url
2. run composer install
3. copy pase .env.sample and rename it to .env
4. Change the values in .env file accordingly
5. run php artisan key:generate to generate the APP_KEY
